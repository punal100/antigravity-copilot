{
  "name": "antigravity-copilot",
  "displayName": "Antigravity for Copilot",
  "description": "Use Google Antigravity models (Claude, Gemini) in GitHub Copilot Chat via CLIProxyAPI",
  "version": "1.3.0",
  "publisher": "punal100",
  "icon": "media/icon.png",
  "engines": {
    "vscode": "^1.95.0"
  },
  "categories": [
    "AI",
    "Chat",
    "Machine Learning",
    "Programming Languages"
  ],
  "keywords": [
    "antigravity",
    "copilot",
    "claude",
    "gemini",
    "ai",
    "cli-proxy-api"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "antigravity-copilot.startServer",
        "title": "Start Antigravity Server",
        "category": "Antigravity"
      },
      {
        "command": "antigravity-copilot.stopServer",
        "title": "Stop Antigravity Server",
        "category": "Antigravity"
      },
      {
        "command": "antigravity-copilot.restartServer",
        "title": "Restart Antigravity Server",
        "category": "Antigravity"
      },
      {
        "command": "antigravity-copilot.showServerControls",
        "title": "Show Server Controls",
        "category": "Antigravity"
      },
      {
        "command": "antigravity-copilot.configureModels",
        "title": "Configure Copilot Models",
        "category": "Antigravity"
      },
      {
        "command": "antigravity-copilot.loginAntigravity",
        "title": "Login to Antigravity",
        "category": "Antigravity"
      },
      {
        "command": "antigravity-copilot.rateLimitStatus",
        "title": "Rate Limit Status",
        "category": "Antigravity"
      }
    ],
    "configuration": {
      "title": "Antigravity for Copilot",
      "properties": {
        "antigravityCopilot.server.enabled": {
          "type": "boolean",
          "default": false,
          "description": "Enable the Antigravity server on startup"
        },
        "antigravityCopilot.server.autoStart": {
          "type": "boolean",
          "default": false,
          "description": "Automatically start the server when VS Code opens"
        },
        "antigravityCopilot.server.executablePath": {
          "type": "string",
          "default": "",
          "description": "Path to cli-proxy-api.exe (leave empty to auto-detect in %USERPROFILE%\\CLIProxyAPI)"
        },
        "antigravityCopilot.server.port": {
          "type": "number",
          "default": 8317,
          "description": "Port number for CLIProxyAPI server"
        },
        "antigravityCopilot.server.host": {
          "type": "string",
          "default": "127.0.0.1",
          "description": "Host address for CLIProxyAPI server"
        },
        "antigravityCopilot.autoConfigureCopilot": {
          "type": "boolean",
          "default": true,
          "description": "Automatically configure Copilot Chat custom models on startup"
        },
        "antigravityCopilot.copilot.providerGroupName": {
          "type": "string",
          "default": "Antigravity",
          "description": "Name of the Copilot 'OpenAI Compatible' provider group to create/update (this appears in chatLanguageModels.json)."
        },
        "antigravityCopilot.showNotifications": {
          "type": "boolean",
          "default": true,
          "description": "Show notifications for server events"
        },
        "antigravityCopilot.rateLimit.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable rate limiting to prevent 429 errors with Antigravity models"
        },
        "antigravityCopilot.rateLimit.cooldownMs": {
          "type": "number",
          "default": 15000,
          "minimum": 1000,
          "maximum": 120000,
          "description": "Cooldown period in milliseconds between requests"
        },
        "antigravityCopilot.rateLimit.showNotifications": {
          "type": "boolean",
          "default": true,
          "description": "Show notifications when requests are blocked due to rate limiting"
        },
        "antigravityCopilot.proxy.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable a local throttling proxy (queues Copilot requests to reduce upstream 429s). When enabled, configured model URLs will point to this proxy instead of directly to CLIProxyAPI."
        },
        "antigravityCopilot.proxy.host": {
          "type": "string",
          "default": "127.0.0.1",
          "description": "Bind host for the throttling proxy server"
        },
        "antigravityCopilot.proxy.port": {
          "type": "number",
          "default": 8320,
          "minimum": 1024,
          "maximum": 65535,
          "description": "Bind port for the throttling proxy server"
        },
        "antigravityCopilot.proxy.rewriteMaxTokens": {
          "type": "boolean",
          "default": true,
          "description": "When enabled, the proxy will clamp max output tokens in requests to reduce long generations that often trigger upstream 429s."
        },
        "antigravityCopilot.proxy.truncateToolOutput": {
          "type": "boolean",
          "default": true,
          "description": "When enabled, the proxy will truncate very large tool outputs in the prompt history (e.g., git diff) to reduce context size and avoid upstream quota exhaustion."
        },
        "antigravityCopilot.proxy.maxToolOutputChars": {
          "type": "number",
          "default": 12000,
          "minimum": 1000,
          "maximum": 200000,
          "description": "Maximum characters to keep for each tool output message after truncation."
        },
        "antigravityCopilot.proxy.toolOutputHeadChars": {
          "type": "number",
          "default": 6000,
          "minimum": 0,
          "maximum": 200000,
          "description": "Characters to keep from the start of a tool output when truncating."
        },
        "antigravityCopilot.proxy.toolOutputTailChars": {
          "type": "number",
          "default": 2000,
          "minimum": 0,
          "maximum": 200000,
          "description": "Characters to keep from the end of a tool output when truncating."
        },
        "antigravityCopilot.proxy.maxRequestBodyBytes": {
          "type": "number",
          "default": 10485760,
          "minimum": 262144,
          "maximum": 52428800,
          "description": "Absolute maximum request body size (bytes) the proxy will accept. Requests larger than this return 413 to prevent memory issues."
        },
        "antigravityCopilot.proxy.maxTokensThinking": {
          "type": "number",
          "default": 1024,
          "minimum": 256,
          "maximum": 8192,
          "description": "Maximum output tokens to allow for Thinking models when proxy rewriting is enabled. Lower values reduce quota usage."
        },
        "antigravityCopilot.proxy.maxTokensStandard": {
          "type": "number",
          "default": 4096,
          "minimum": 256,
          "maximum": 8192,
          "description": "Maximum output tokens to allow for non-Thinking models when proxy rewriting is enabled"
        },
        "antigravityCopilot.proxy.logRequests": {
          "type": "boolean",
          "default": true,
          "description": "Log proxy request metadata (model, endpoint, token limits, status, duration). Does NOT log prompt text."
        },
        "antigravityCopilot.proxy.transformThinking": {
          "type": "boolean",
          "default": true,
          "description": "Transform streaming responses from Thinking models to annotate reasoning_content for better client display"
        },
        "antigravityCopilot.proxy.thinkingTransformMode": {
          "type": "string",
          "enum": [
            "annotate",
            "enhanced",
            "claude"
          ],
          "default": "annotate",
          "description": "Transform mode for thinking content in streaming responses",
          "enumDescriptions": [
            "Minimal: Add _is_thinking markers to delta objects",
            "Enhanced: Add comprehensive thinking block markers in OpenAI format",
            "Claude: Full conversion to Anthropic/Claude streaming format (experimental)"
          ]
        },
        "antigravityCopilot.proxy.thinkingTimeoutMs": {
          "type": "number",
          "default": 60000,
          "minimum": 10000,
          "maximum": 300000,
          "description": "Timeout in milliseconds for Thinking model requests. Aborts long-running requests to prevent quota exhaustion. (default: 60s)"
        },
        "antigravityCopilot.proxy.requestTimeoutMs": {
          "type": "number",
          "default": 120000,
          "minimum": 10000,
          "maximum": 300000,
          "description": "Timeout in milliseconds for standard model requests. (default: 120s)"
        },
        "antigravityCopilot.proxy.thinkingConcurrency": {
          "type": "number",
          "default": 1,
          "minimum": 1,
          "maximum": 5,
          "description": "Maximum concurrent requests for Thinking models. Keep low (1-2) to avoid upstream quota exhaustion."
        },
        "antigravityCopilot.proxy.standardConcurrency": {
          "type": "number",
          "default": 3,
          "minimum": 1,
          "maximum": 10,
          "description": "Maximum concurrent requests for standard (non-Thinking) models."
        },
        "antigravityCopilot.proxy.maxRetries": {
          "type": "number",
          "default": 3,
          "minimum": 0,
          "maximum": 10,
          "description": "Maximum retry attempts for 429/RESOURCE_EXHAUSTED errors. Set to 0 to disable retries."
        },
        "antigravityCopilot.proxy.retryBaseDelayMs": {
          "type": "number",
          "default": 1000,
          "minimum": 100,
          "maximum": 10000,
          "description": "Base delay (ms) before first retry. Subsequent retries use exponential backoff with jitter."
        }
      }
    },
    "viewsContainers": {
      "activitybar": [
        {
          "id": "antigravity-copilot",
          "title": "Antigravity",
          "icon": "media/icon.svg"
        }
      ]
    },
    "views": {
      "antigravity-copilot": [
        {
          "type": "webview",
          "id": "antigravity-copilot.sidebarView",
          "name": "Dashboard"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "node esbuild.js",
    "watch": "node esbuild.js --watch",
    "package": "vsce package",
    "lint": "eslint src --ext ts"
  },
  "devDependencies": {
    "@types/node": "^20.x",
    "@types/vscode": "^1.95.0",
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "esbuild": "^0.27.2",
    "eslint": "^8.56.0",
    "typescript": "~5.5.4",
    "@vscode/vsce": "^2.22.0"
  },
  "repository": "https://github.com/punal100/antigravity-copilot",
  "homepage": "https://github.com/punal100/antigravity-copilot#readme",
  "bugs": "https://github.com/punal100/antigravity-copilot/issues",
  "license": "MIT"
}
